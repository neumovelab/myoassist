# Development dependencies. Install with:
#   uv pip install -e . -r requirements-dev.txt

-r requirements.txt

pytest>=7.0
ruff==0.15.20  # pinned: ruff's formatter changes between minor releases; keep CI == local
